# RicochetAngles Godot Repository Instructions

## 1. Project Purpose

- 이 저장소는 HTML/Canvas 2D 프로토타입으로 검증한 전차 전투 게임 `RicochetAngles`를 Godot 4 기반의 독립적인 3D 게임으로 이식하는 프로젝트다.
- 단기 목표는 기존 프로토타입의 이동, 장갑, 도탄, 관통, 약점, 부위 파괴, 적 AI, 곡사포, 불렛타임의 게임성을 보존하는 것이다.
- 장기 목표는 로폴리 3D 렌더링, 높이가 있는 지형, 공중 유닛, 다양한 스테이지와 확장 콘텐츠를 수용할 수 있는 단독 3D 게임 기반을 만드는 것이다.
- 정확한 HTML 픽셀 좌표, Canvas 렌더링 방식, 단일 파일 구조를 복제하는 것이 목적은 아니다.
- 플레이 가능성, 판정 가독성, 검증 가능성을 그래픽 완성도와 과도한 추상화보다 우선한다.
- 사용자 요청 범위 밖의 기능, 시스템, 플러그인, 외부 의존성 또는 구조 개편을 임의로 추가하지 않는다.

## 2. Repository Scope

- 이 `AGENTS.md`는 저장소 루트부터 하위 전체에 적용한다.
- 더 하위 폴더에 별도의 `AGENTS.md`가 명시적으로 추가되면 해당 범위에서는 더 가까운 문서가 우선한다.
- 현재 저장소는 Godot 프로젝트 전용이다.
- 기존 HTML `index.html`을 이 저장소에 복사하거나 런타임에 포함하지 않는다.
- HTML, JavaScript, Web Audio 코드는 참고 자료이며 Godot 프로젝트의 실행 의존성이 아니다.

## 3. Source of Truth

우선순위는 다음과 같다.

1. 현재 Godot 저장소 브랜치의 실제 코드와 씬
2. 이 `AGENTS.md`의 개발 계약
3. `MIGRATION_PHASES.md`의 단계별 범위와 완료 조건
4. HTML 기준 저장소 `series341000Lunar/steel-angle-prototype`
5. HTML 기준 커밋 `c99ccd2b6628f1560e2340899f2f0715675570d6`
6. 기준 플레이 영상과 사용자 확인 내용

- 현재 Godot 코드와 문서가 충돌하면 작업 전에 차이를 보고한다.
- 사용자 승인 없이 코드를 문서에 맞추거나 문서를 코드에 맞춰 임의 수정하지 않는다.
- HTML 구현은 게임 규칙과 검증된 동작의 참고 기준이다.
- HTML의 픽셀 수치, 함수명, 전역 객체 구조, 렌더 순서까지 1:1로 복제하지 않는다.
- 플레이 영상은 조작감, 속도감, 경고 연출, UI 가독성의 보조 기준으로 사용한다.
- 구현되지 않은 기능을 대화, 계획 또는 문서에만 있다는 이유로 구현 완료로 기록하지 않는다.

## 4. Status Vocabulary

게임 기능과 검증 상태는 다음으로 구분한다.

- `IMPLEMENTED`: 실제 코드와 씬에 연결되어 실행 확인까지 완료됨
- `PARTIAL`: 일부 경로만 구현되었거나 주요 예외가 남아 있음
- `PLANNED`: 설계 또는 단계 문서에만 존재함
- `UNVERIFIED`: 코드가 있어도 실행 또는 수동 검증을 수행하지 못함
- `DEFERRED`: 현재 이식 범위에서 의도적으로 뒤 단계로 미룸

- 문서 작성만 완료된 상태를 게임 기능 `IMPLEMENTED`로 표시하지 않는다.
- 실행하지 않은 검사를 통과했다고 쓰지 않는다.

## 5. Current Technology Contract

- Godot 4.7 일반 버전을 기준으로 한다.
- 스크립트 언어는 GDScript를 기본으로 한다.
- 렌더러는 현재 `Compatibility`를 유지한다.
- 프로젝트 공간은 3D다.
- 렌더링은 로폴리 3D를 기본 방향으로 한다.
- 카메라는 직교 투영 `Camera3D`를 기본으로 한다.
- 지상 이동은 XZ 평면을 사용하고 Y축은 높이로 사용한다.
- 공간 단위는 `1 Godot unit = 1 meter`로 간주한다.
- 장갑 두께와 관통력은 밀리미터 단위로 관리한다.
- MCP와 에디터 플러그인은 현재 사용하지 않는다.
- 외부 애드온, 네이티브 확장, C# 전환, 별도 서버, 네트워크 백엔드는 사용자 승인 없이 추가하지 않는다.

## 6. Long-Term 3D Direction

- 초기 플레이는 평면적인 탑다운 전투로 제한할 수 있지만 전체 기반은 3D로 유지한다.
- 공중 유닛은 향후 실제 Y 고도를 사용한다.
- 지형 높이, 건물, 장애물, 고가 구조물 등은 3D 공간에서 확장 가능해야 한다.
- 2D 좌표와 별도 가상 고도를 병행하는 이중 좌표 체계를 기본 구조로 만들지 않는다.
- 단기 이식을 위해 장기 3D 확장 가능성을 막는 구조를 선택하지 않는다.
- 반대로 장기 확장 가능성을 이유로 현재 필요하지 않은 시스템을 선행 구현하지 않는다.

## 7. Coordinate and Unit Rules

- X축은 좌우, Y축은 높이, Z축은 전후로 사용한다.
- 지상 전차의 수평 이동과 차체 선회는 XZ 평면에서 처리한다.
- 전차의 회전 기준은 Y축 yaw다.
- 포탑 yaw는 차체 yaw와 분리한다.
- 거리, 속도, 폭발 반경, 카메라 높이, 비행 고도는 미터 기준이다.
- 장갑과 관통력은 mm 기준이다.
- 시간은 초 단위다.
- 각도 계산 내부 표현은 라디안을 기본으로 사용하되, 튜닝과 디버그 표시는 필요에 따라 도 단위를 허용한다.
- 미터와 mm를 암묵적으로 혼합하지 않는다.
- HTML의 1픽셀을 Godot 1미터로 변환하지 않는다.
- 월드 크기, 전차 크기, 속도는 Phase 1 플레이테스트에서 잠정값을 정하고 이후 튜닝할 수 있게 유지한다.

## 8. World and Stage Rules

- 초기 스테이지는 유한한 3D 월드로 만든다.
- 최초 월드 크기를 프로젝트 전체의 최대 크기로 간주하지 않는다.
- 스테이지마다 다른 월드 경계를 허용한다.
- 월드 경계는 미터 단위 데이터로 관리한다.
- 기본 지상 경계는 XZ 범위로 표현한다.
- Y축은 지형 높이와 공중 고도에 사용한다.
- 초기 월드는 단순한 평면일 수 있지만 지형 높이 확장을 막지 않는다.
- HTML 월드의 3840×3600 픽셀 수치는 절대 크기로 이식하지 않는다.
- 보존할 대상은 전투 거리, 화면에 보이는 범위, 월드 횡단 시간, 장애물 밀도 같은 상대적 게임 감각이다.

## 9. Camera Rules

- 기본 카메라는 직교 투영 `Camera3D`다.
- 카메라는 기울어진 탑다운 구도를 사용해 전차의 상면과 측면 높이가 함께 보이게 한다.
- 정확한 pitch, yaw, orthogonal size는 Phase 1에서 정한다.
- 카메라 회전은 기본적으로 고정한다.
- 플레이어 중심을 XZ 방향으로 추적한다.
- 진행 방향 look-ahead를 기본 기능으로 추가하지 않는다.
- 월드 또는 스테이지 경계에서 카메라를 제한한다.
- HUD는 `CanvasLayer` 등 화면 좌표 계층에 두어 카메라 이동의 영향을 받지 않게 한다.
- 마우스 조준은 화면 좌표에서 3D 레이를 생성하여 지면 또는 명시적인 조준 평면과 교차시킨 월드 지점을 사용한다.
- 카메라가 이동해도 포탑 조준점이 틀어지지 않아야 한다.
- 향후 공중 표적 조준은 실제 3D 표적 선택으로 확장할 수 있어야 한다.

## 10. Vehicle Architecture Rules

- 플레이어와 이동형 전차는 우선 스크립트 제어형 `CharacterBody3D` 계열로 구현한다.
- `VehicleBody3D`, 실제 휠 서스펜션, 실제 무한궤도 물리는 기본 구현으로 사용하지 않는다.
- 전차의 가속, 자연 감속, 역방향 제동, 후진, 선회 감각은 명시적인 게임 로직으로 계산한다.
- 물리 엔진의 마찰, 질량, 관성에 조작감을 전적으로 맡기지 않는다.
- 차체 방향과 포탑 방향을 독립적으로 관리한다.
- 전진 속도, 목표 속도, 실제 위치 변화에서 계산한 속도를 구분한다.
- 충돌 시 속도를 즉시 또는 크게 줄이는 기존 게임 감각을 보존한다.
- 후진 중 선회 방향 반전 여부는 기존 HTML 동작을 기준으로 한다.
- 횡이동은 사용자 요청 없이 추가하지 않는다.

## 11. Track Rules

- 무한궤도는 실제 물리 시뮬레이션 대상이 아니다.
- 시각적 궤도 움직임은 UV 이동, 바퀴 회전, 간단한 메시 애니메이션, 먼지 파티클 등으로 표현할 수 있다.
- 좌우 궤도는 별도의 파괴 가능한 게임플레이 모듈이다.
- 궤도 파괴 시 이동과 선회 성능이 변경된다.
- 궤도 복구 시간과 폭주 상태의 복구 배율을 보존한다.
- 그래픽상 궤도 연출과 게임플레이상 궤도 HP 및 상태를 분리한다.

## 12. Rendering and Collider Separation

- 렌더링 메시를 기본 전투 판정 콜라이더로 사용하지 않는다.
- 복잡한 imported mesh collision을 기본값으로 생성하지 않는다.
- 렌더링 메시와 이동용 콜라이더를 분리한다.
- 이동용 콜라이더와 장갑 판정용 콜라이더를 분리한다.
- 이동용 콜라이더는 단순한 Box, Capsule 또는 소수의 Convex 형태를 우선한다.
- 장갑 구역은 단순한 여러 개의 `CollisionShape3D` 또는 동등한 명시적 판정 구조를 사용한다.
- 메시 교체나 로폴리 디테일 변경으로 게임 판정이 바뀌지 않아야 한다.
- 콜라이더는 디버그에서 직접 식별하고 표시할 수 있어야 한다.

## 13. Armor Zone Contract

전차는 필요에 따라 다음 장갑 구역을 가질 수 있다.

- 차체 전면
- 차체 좌측
- 차체 우측
- 차체 후면
- 포탑 전면
- 포탑 좌측
- 포탑 우측
- 포탑 후면
- 좌측 궤도
- 우측 궤도
- 약점
- 엔진 또는 기타 모듈

각 장갑 구역은 최소한 다음 메타데이터를 제공해야 한다.

- `zone_id`
- `zone_type`
- `armor_mm`
- `module_id`
- `weakpoint_id`
- 활성 또는 비활성 상태
- 피해 가능 여부

- 정확한 노드명과 파일명은 해당 Phase에서 확정한다.
- 장갑 구역 판정은 렌더 메시의 삼각형 재질이나 이름에 암묵적으로 의존하지 않는다.

## 14. Collision Layer Responsibilities

정확한 번호는 구현 Phase에서 정하지만 책임은 분리한다.

- 월드 지형과 고정 장애물
- 전차 이동 충돌
- 장갑 구역
- 보병
- 아이템
- 포탄 공간 쿼리
- 탐지 및 특수 트리거
- 향후 공중 유닛

- 충돌 레이어와 마스크는 목적이 드러나게 관리한다.
- 같은 콜라이더를 이동, 장갑, 탐지, 아이템 수집의 모든 용도로 무리하게 재사용하지 않는다.
- 새 레이어를 추가할 때 기존 쿼리와 마스크의 회귀 여부를 확인한다.

## 15. Projectile Collision Rules

- 빠른 포탄은 단순 overlap 이벤트에만 의존하지 않는다.
- 이전 프레임 위치와 현재 예정 위치 사이를 3D ray query 또는 작은 shape cast로 검사한다.
- 여러 후보가 있으면 가장 가까운 최초 충돌 하나를 처리한다.
- 배열 순서, 노드 트리 순서, 생성 순서로 피해 대상을 결정하지 않는다.
- 충돌 결과에서 대상, 충돌 위치, 표면 노멀, 충돌 거리 또는 fraction을 확보한다.
- 장애물이 먼저 맞으면 뒤의 전차나 보병에 피해를 주지 않는다.
- 기관총의 시각적 예광선과 실제 판정은 같은 시작점, 방향, 산포 표본, 끝점을 사용한다.
- 폭발은 구형 범위 쿼리를 기본으로 한다.
- 도탄 포탄은 충돌 노멀로 반사하고 충돌면 바깥의 안전한 위치에서 계속 비행한다.
- 동일 표면에 즉시 재충돌하지 않도록 작은 separation offset 또는 동등한 보호를 둔다.
- 최대 도탄 횟수를 유지한다.
- 실제 판정과 불렛타임 예측은 같은 충돌 및 장갑 계산 함수를 공유해야 한다.

## 16. Attack Identity Contract

모든 포탄과 공격에는 초기부터 다음 정보를 유지한다.

- `projectile_id`
- `source_id`
- `source_type`
- `source_faction`
- `weapon_type`
- `ammo_type`

충돌 및 피해 결과에는 최소한 다음 정보를 제공한다.

- `target_id`
- `target_type`
- `armor_zone_id`
- `hit_position`
- `hit_normal`
- `impact_angle`
- `armor_mm`
- `effective_armor_mm`
- `penetration_mm`
- `result`
- `reason`
- `friendly_fire`

이 정보는 다음 시스템에서 공통으로 사용한다.

- 피해
- 도탄
- SCORE
- XP
- 적 아군 오사
- 약점 공격
- 은행사격 보상
- 불렛타임 추적
- 디버그 표시
- 아군 곡사포 barrage 제한

## 17. Armor and Penetration Rules

- Godot 물리는 충돌 여부, 위치, 노멀을 제공한다.
- 관통, 비관통, 도탄 결과는 별도의 계산 계층에서 결정한다.
- 입사각은 장갑 노멀 기준으로 계산한다.
- `0°`는 장갑면에 수직으로 명중한 상태다.
- `90°`는 장갑면을 스치듯 명중한 상태다.
- APHE와 APCR은 입사각을 먼저 계산한다.
- 75도 이상은 장갑과 관통력에 관계없이 자동 도탄이 우선한다.
- 도탄 가능 횟수가 남지 않았을 때의 최종 처리도 실제와 예측에서 동일해야 한다.
- 유효 장갑과 거리별 관통력 감소를 계산한다.
- 장갑과 관통 수치는 mm 단위다.
- 장갑 계산은 가능한 한 부작용 없는 순수 함수로 유지한다.
- UI와 디버그는 실제 계산에 사용한 동일 결과 객체를 사용한다.
- 실제 판정과 예측 판정을 별도로 재구현하지 않는다.

## 18. Ammo Rules

### APHE

- 기본 탄종이다.
- 무한 탄약이다.
- 관통, 비관통, 자동 도탄, 반사, 궤도, 약점 규칙을 사용한다.

### APCR

- 제한 탄약이다.
- 시작 탄약 수는 기존 HTML 계약을 기준으로 한다.
- 기본탄보다 빠르다.
- 75도 이상에서는 자동 도탄한다.
- 75도 미만에서는 기존 프로토타입과 동일하게 강제 관통 규칙을 사용한다.
- 보스 궤도 후보를 건너뛴다.
- trigger 약점 단계에서는 엔진 노출로 단축한다.
- engine 단계에서는 엔진 직접 명중만 HP 피해로 인정한다.

### HE

- 제한 탄약이다.
- 장갑 관통 및 도탄 계산을 수행하지 않는다.
- 최초 충돌, 수명 종료 또는 월드 경계 처리에서 한 번만 폭발한다.
- 폭발 시작 시 약점 상태를 snapshot한다.
- 하나의 폭발이 trigger 약점과 새로 열린 엔진을 연속 처리하지 못하게 한다.
- 보스 궤도, 활성 약점, 보병, 대전차포에 적용한다.
- 경전차 본체의 기본 피해 규칙은 기존 HTML 계약을 따른다.
- 특수탄은 실제 발사체 생성 성공 후에만 감소한다.
- 발사 실패 시 탄약, 재장전, 산포 bloom을 소비하지 않는다.

## 19. Player Gameplay Invariants

- W/S는 차체 정면 기준 전진과 후진이다.
- A/D는 차체 선회다.
- 횡이동을 추가하지 않는다.
- 마우스는 포탑 조준이다.
- 좌클릭은 주포, 우클릭은 기관총이다.
- 차체와 포탑 방향은 독립이다.
- 이동, 장거리 고속 상태, 제압, 발사 bloom, 연막이 산포에 영향을 준다.
- 연막 상태의 최종 산포는 기존 계약의 45도 override다.
- GAS는 `5`, `Digit5`, `Numpad5` 입력으로 1개를 소비해 HP를 최대 1 회복한다.
- `M`은 전체 사운드 음소거 및 해제다.
- `R`은 매치 전체를 재시작한다.
- 불렛타임과 아군 포격 조준 중 허용 입력은 각 Phase 계약을 따른다.

## 20. Boss Gameplay Invariants

- 보스 최대 HP는 10이다.
- 활성 엔진 처리만 HP를 정확히 1 감소시킨다.
- 비치명 약점은 cupola, gun port, vision slit 중 하나만 활성화한다.
- trigger 약점 관통은 HP를 줄이지 않고 엔진을 노출한다.
- 엔진 노출은 최대 10초다.
- 엔진 노출 초기 stagger 동안 이동과 선회가 크게 감소한다.
- 엔진 관통 또는 제한시간 종료 뒤 다음 trigger 약점을 선택한다.
- 같은 프레임의 엔진 관통을 타이머 만료보다 우선한다.
- HP가 처음 5 이하가 되면 폭주 모드에 진입한다.
- 폭주는 이동, 선회, 재장전, 궤도 복구, 곡사포, 약점 변경에 영향을 준다.
- 좌우 궤도는 별도 HP와 복구 상태를 가진다.
- 보스 자체는 적 아군 오사 피해와 약점 변화를 받지 않는다.
- 승리 후 신규 공격, 생성, 리스폰을 중단한다.

## 21. Enemy and Respawn Invariants

- periodic 경전차는 최대 1대를 기본 계약으로 한다.
- periodic 경전차는 보스 주변에 합류하고 플레이어를 탐지한 뒤 추격한다.
- anti-orbit 경전차는 보스-플레이어 상대 운동의 누적 회전을 기준으로 생성한다.
- anti-orbit는 3회전 이후 경고를 거쳐 2개 interceptor 슬롯을 사용한다.
- 각 interceptor 슬롯은 독립적인 리스폰 타이머를 가진다.
- 대전차포는 고정 슬롯, HP 2, 전차포 전용, 파괴 후 리스폰, APCR 드롭 규칙을 따른다.
- 보병은 고정 분대, 분대당 5명, 제압사격 전용, 전멸 후 리스폰, HE 드롭 규칙을 따른다.
- 보병과 보스 기관총은 플레이어 HP를 직접 감소시키지 않고 제압을 적용한다.
- 리스폰 위치가 막히면 전체 쿨다운을 다시 시작하지 않고 짧게 재시도한다.
- 플레이어, 보스, 살아 있는 전차, 장애물과의 겹침을 생성 전에 검사한다.
- 승리 또는 패배 후 모든 신규 생성과 리스폰을 중단한다.

## 22. Pickup, Score, and Experience Invariants

- 대전차포 파괴는 APCR 1개를 드롭한다.
- 보병 분대 전멸은 HE 1개를 드롭한다.
- 경전차 드롭은 기존 성공 확률과 APCR/HE/GAS 가중치를 기준으로 한다.
- 같은 종류 pickup이 가까우면 수량을 병합한다.
- pickup은 월드 좌표를 사용한다.
- 장애물 내부에 생성되면 안전한 인접 위치로 보정한다.
- pickup은 플레이어와의 충돌 또는 수집 범위로 획득한다.
- SCORE와 XP는 별도 값으로 유지한다.
- SCORE, XP, 탄약, GAS는 현재 매치 상태이며 영구 저장하지 않는다.
- 적 파괴 SCORE는 생명주기당 한 번만 지급한다.
- 적 아군 오사로 일반 적이 파괴되면 별도 XP 규칙을 적용한다.
- 적 주포탄의 실제 도탄과 비관통 방어 보상은 중복 지급하지 않는다.
- 도탄 후 활성 약점 관통의 bank-shot 보상은 포탄당 한 번만 지급한다.

## 23. Time System Contract

처음부터 다음 delta 개념을 분리한다.

- 실제 시간 `real_delta`
- 시뮬레이션 시간 `simulation_delta`
- 플레이어 이동 시간 `player_move_delta`
- 플레이어 선회 시간 `player_turn_delta`

시간 모드는 다음과 같이 상호 배타적으로 관리한다.

- `NORMAL`
- `BULLET_TIME`
- `SKILL_TARGETING`

- 불렛타임을 전역 `Engine.time_scale` 하나만으로 구현하지 않는다.
- 실시간 기준 타이머와 시뮬레이션 기준 타이머를 명시적으로 구분한다.
- 불렛타임은 실시간 2초다.
- 적과 포탄, 플레이어 이동, 플레이어 선회는 서로 다른 시간 배율을 사용할 수 있다.
- UI와 일부 입력은 실제 시간을 유지한다.
- 아군 포격 조준 모드는 게임 감속 상태가 아니다.
- 랜덤 쿨다운은 상태 진입 또는 burst 종료 시 한 번만 뽑고 매 프레임 재추첨하지 않는다.

## 24. State Management Rules

주요 복수 단계 시스템은 명시적인 enum 또는 상태값을 사용한다.

예:

- 매치: `PLAYING`, `WON`, `LOST`
- 시간 모드: `NORMAL`, `BULLET_TIME`, `SKILL_TARGETING`
- 공격: `IDLE`, `AIM`, `WARNING`, `FIRING`, `RELOAD`
- 약점: `TRIGGER_WEAKPOINT`, `ENGINE_EXPOSED`
- 연막: `COOLDOWN`, `TELEGRAPH`, `ACTIVE`

- 같은 의미를 여러 boolean으로 중복 관리하지 않는다.
- 한 프레임에 여러 상태 전이가 겹칠 경우 우선순위를 명시한다.
- 단발 이벤트의 중복 처리를 막는 플래그 또는 이벤트 ID를 사용한다.
- 렌더링과 UI 갱신에서 게임 상태를 변경하지 않는다.

## 25. Audio Contract

- Godot 오디오 버스 기반으로 구성한다.
- 마스터 음소거는 매치 상태와 분리한다.
- `R` 재시작으로 음소거 상태를 초기화하지 않는다.
- 초기에는 단순 전자음 또는 임시 효과음을 사용할 수 있다.
- 최종 원음 에셋은 현재 Phase의 필수 조건이 아니다.
- 동시 재생 제한, 거리 감쇠, 우선순위가 필요하면 중앙 오디오 관리 계층에서 처리한다.
- 사운드 재생 실패가 게임 로직을 중단시키지 않아야 한다.

## 26. Tuning and Data Rules

- 속도, 시간, HP, 장갑, 관통력, 산포, 쿨다운, 반경, 리스폰 시간 등 조정 가능한 수치는 중앙 튜닝 데이터에 둔다.
- 같은 의미의 값을 중복 정의하지 않는다.
- 파생값은 원본 설정에서 계산한다.
- 게임플레이 튜닝과 오디오 튜닝을 분리한다.
- 스테이지 크기와 카메라 값은 데이터화한다.
- 초기에는 과도한 Resource 세분화를 피하되 향후 `.tres` 등으로 이전 가능하게 한다.
- 미사용 legacy 값이나 HTML 변수명을 실제 계약으로 오해하지 않는다.
- HTML 수치를 변환할 때 단순 픽셀-미터 비례가 아니라 게임 감각을 기준으로 재조정한다.

## 27. Scene Responsibility Guidelines

정확한 파일명과 노드명은 Phase별로 확정하지만 책임은 다음처럼 분리한다.

- 최상위 매치와 재시작
- 3D 월드
- 플레이어와 적 유닛
- 포탄과 폭발
- 생성과 리스폰
- 시간 모드
- 카메라
- 오디오
- 화면 UI와 디버그

- 하나의 거대한 스크립트로 모든 책임을 다시 합치지 않는다.
- 반대로 작은 기능마다 파일을 만들어 과도하게 분산하지 않는다.
- 씬, 스크립트, 데이터의 책임 경계를 작업 전에 제시한다.
- 아직 존재하지 않는 파일명과 함수명을 문서만으로 강제하지 않는다.

## 28. Reset and Match-End Rules

`R` 재시작은 다음을 초기화한다.

- 플레이어
- 보스
- 일반 적
- 포탄
- 폭발과 파티클
- 장애물 상태
- 약점
- 궤도
- 곡사포
- SCORE와 XP
- 탄약과 GAS
- pickup
- 생성 및 리스폰 슬롯
- 불렛타임 추적 상태
- 아군 포격 상태
- anti-orbit 누적 상태
- 경고와 단발 이벤트

다음은 유지한다.

- 사운드 음소거
- 오디오 시스템
- 프로그램 또는 사용자 설정

- 승리와 패배 후 신규 공격, 생성, 리스폰, pickup 생성과 수집을 중단한다.
- R만으로 깨끗한 새 매치를 시작할 수 있어야 한다.
- 씬 재로드를 사용하더라도 매치 외 상태가 의도치 않게 초기화되지 않아야 한다.

## 29. Debug and Observability Rules

각 Phase에서 핵심 상태를 직접 확인할 수 있어야 한다.

초기부터 필요한 정보:

- 플레이어 월드 위치
- 차체 방향
- 포탑 방향
- 현재 전진 속도
- 실제 속도
- 카메라 위치와 직교 크기
- 마우스 월드 조준점
- 매치 상태
- 시간 모드

포탄 Phase부터:

- 포탄 ID
- 발사자와 진영
- 이전 위치와 현재 위치
- 최초 충돌 대상
- 충돌점
- 충돌 노멀
- 충돌 거리 또는 fraction

장갑 Phase부터:

- 장갑 구역
- 입사각
- 장갑 두께
- 유효 장갑
- 현재 관통력
- 결과와 reason
- 도탄 반사 방향과 남은 횟수

이후:

- 약점 상태
- AI 상태
- 리스폰 상태
- 곡사포 타이머
- 불렛타임 예측과 실제 결과 비교

- 디버그 UI는 실제 계산 결과를 재사용한다.
- 디버그용 계산을 별도로 재구현하지 않는다.

## 30. Migration Phase Gating

- 구현 순서는 `MIGRATION_PHASES.md`를 따른다.
- 사용자가 명시적으로 다음 Phase의 구현을 승인하기 전까지 코드를 시작하지 않는다.
- 현재 Phase 범위 밖의 기능을 선행 구현하지 않는다.
- 다음 Phase에 필요한 인터페이스는 최소한으로 준비할 수 있지만 실제 기능을 몰래 완성하지 않는다.
- 각 Phase 작업 전 범위, 수정 대상, 검증 방법을 먼저 제시한다.
- 각 Phase 완료 후 사용자 검증과 승인 없이 다음 Phase로 넘어가지 않는다.
- 큰 구조 변경이 필요하면 현재 Phase를 `PARTIAL`로 남기고 차이를 보고한다.

## 31. Required Workflow for Every Change

1. 현재 브랜치와 작업 트리를 확인한다.
2. 관련 씬, 스크립트, 데이터, 입력, 충돌 레이어를 읽는다.
3. 현재 동작과 요청 범위를 요약한다.
4. 수정할 파일과 책임을 제시한다.
5. 최소 변경 계획을 제시한다.
6. 사용자 승인 범위 안에서만 수정한다.
7. Godot 프로젝트 로드와 실행 오류를 확인한다.
8. Phase별 수동 테스트를 수행한다.
9. 변경 파일, 주요 상태, 보존한 동작, 검사 결과, 미검증 항목을 보고한다.
10. 요청하지 않은 commit, push, branch 변경을 하지 않는다.

## 32. Validation Rules

- Godot 프로젝트가 해당 환경의 `godot` 명령으로 열리는지 확인한다.
- 오류, 파서 에러, 누락 리소스, 잘못된 노드 경로를 확인한다.
- Phase별 수동 테스트 목록을 실행한다.
- 프레임레이트 차이로 이동과 타이머가 달라지지 않는지 확인한다.
- 충돌 변경은 이동 겹침, 장애물 차단, 포탄 최초 충돌을 검사한다.
- 카메라 변경은 추적, 경계, 마우스 조준, HUD 고정을 검사한다.
- 장갑 변경은 전면, 측면, 후면, 도탄, 비관통, 관통, 반사 후 재충돌을 검사한다.
- 상태 변경은 생성, update, UI, 승패 중단, R reset을 모두 검사한다.
- 실행하지 못한 항목은 이유와 수동 확인 방법을 보고한다.
- 경고를 오류처럼 과장하지 않되 반복 오류를 방치하지 않는다.

## 33. Git and File Safety

- 작업 전후 `git status --short`와 현재 브랜치를 확인한다.
- 사용자 요청 없이 commit, push, branch 생성 또는 전환을 하지 않는다.
- `reset --hard`, `clean -fd`, rebase, force push를 하지 않는다.
- 기존 uncommitted 변경을 덮어쓰거나 되돌리지 않는다.
- 관련 없는 파일을 수정하지 않는다.
- `.godot/`와 생성 캐시를 추적하지 않는다.
- 임시 파일과 테스트 생성물을 보고 없이 남기지 않는다.
- 대형 바이너리와 원본 에셋을 추가하기 전에 저장 방식과 용량 영향을 보고한다.
- 프로젝트 경로는 PC마다 다를 수 있으므로 로컬 절대 경로를 저장소 계약에 박아 넣지 않는다.

## 34. Plugins, MCP, and Dependencies

- 현재 MCP를 사용하지 않는다.
- 현재 외부 Godot 플러그인을 사용하지 않는다.
- Codex는 프로젝트 파일을 수정하고 로컬 Godot CLI로 로드 및 실행 검증한다.
- 에디터 GUI 직접 조작을 전제로 하지 않는다.
- 플러그인이나 MCP 도입은 기본 프로젝트와 첫 플레이 가능 버전이 안정된 뒤 별도 검토한다.
- 새 의존성이 필요한 경우 목적, 권한, 유지보수 위험, 제거 가능성을 먼저 보고한다.

## 35. Scope Boundaries and Non-Goals

현재 기본 비목표:

- 실제 무한궤도 및 서스펜션 물리
- 사실적인 차량 시뮬레이터
- 3D 렌더 메시 기반 정밀 삼각형 장갑 판정
- 온라인 멀티플레이
- 서버, 계정, 백엔드
- 영구 진행, 상점, 스테이지 선택
- 대규모 경로 탐색
- 무한 월드 스트리밍
- 신규 적, 무기, 스킬의 임의 추가
- 최종급 그래픽과 최종 사운드 에셋
- HTML 런타임 내장

명시적 사용자 요청이 있기 전까지 비목표를 구현하지 않는다.

## 36. Known Fragile Areas

- 직교 Camera3D의 화면 좌표와 지면 조준점 변환
- 카메라 경계와 월드 경계의 분리
- 차체 yaw와 포탑 yaw의 부모-자식 회전 관계
- CharacterBody3D 이동과 커스텀 가감속의 결합
- 이동용 콜라이더와 장갑 구역 콜라이더의 충돌 마스크
- 빠른 포탄의 3D sweep와 최초 충돌 선택
- 장갑 구역이 겹칠 때 약점, 궤도, 차체의 우선순위
- 포탑 회전 중 장갑 노멀과 약점 위치
- 도탄 후 동일 표면 즉시 재충돌
- 실제와 불렛타임 예측의 함수 공유
- 다중 delta 시간 체계와 타이머 기준
- 승패 후 공격, 생성, 리스폰 중단
- R reset에서 매치 상태 누락
- 아군 오사와 SCORE/XP 중복 지급
- HE 약점 snapshot과 barrage 제한
- 공중 유닛 추가 시 지상 전용 쿼리의 오작동

## 37. Current Migration Status

- Phase 0: 계약 문서 확정
- Godot 게임 코드: 미착수
- 메인 3D 씬: 미착수
- 플레이어 이동: 미착수
- 전투 시스템: 미착수
- HTML 기준 커밋: `c99ccd2b6628f1560e2340899f2f0715675570d6`
- 다음 단계: `MIGRATION_PHASES.md`의 Phase 1 설계 및 사용자 승인

## 38. Definition of Done for a Phase

- 요청한 Phase 범위가 실제 씬과 업데이트 경로에 연결되어 있다.
- 기존 완료 Phase가 회귀하지 않는다.
- 조정 가능한 값이 중앙 튜닝 데이터에 있다.
- 좌표와 단위가 일관된다.
- 이동용 콜라이더와 전투 판정용 콜라이더가 역할에 맞게 분리되어 있다.
- 포탄은 최초 충돌을 일관되게 처리한다.
- 새 상태와 타이머가 승패 중단과 R reset에 포함된다.
- 핵심 상태를 디버그 UI 또는 명시적 로그로 확인할 수 있다.
- Godot 로드 또는 실행 오류가 없다.
- 수행한 테스트, 실패한 테스트, 미검증 항목, 알려진 한계를 보고했다.
- 요청하지 않은 Git 작업과 관련 없는 파일 변경이 없다.
- 사용자가 결과를 확인하고 다음 Phase 진행을 승인했다.
